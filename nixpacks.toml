[phases.setup]
  nixPkgs = ['php83']

[phases.build]
  cmds = [
    'composer install --optimize-autoloader --no-dev --no-interaction',
    'chmod +x railway/init-app.sh'
  ]

[start]
  cmd = "railway/init-app.sh && php artisan serve --host=0.0.0.0 --port=$PORT"
